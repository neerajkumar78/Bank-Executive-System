package Bank;
//import org.hibernate.cfg.*;
//import org.hibernate.*;
import java.util.Scanner;
public class GetData {
	 
	public static void main(String[] args) {
		String isbn;
		String topic;
		String author;
		int price;
		String publisher;
		Scanner scan1=new Scanner(System.in);
		Scanner scan2=new Scanner(System.in);
		/*Configuration cfg=new Configuration();
		cfg.addAnnotatedClass(Main.class);
		SessionFactory factory=cfg.buildSessionFactory();
		Session session=factory.openSession();
		//get data from id
		System.out.println("enter your isbn");
        isbn=scan2.nextLine();
		Main obj=session.load(Main.class,isbn );
        System.out.println("ISBN: "+obj.getIsbn()+" Topic: "+obj.getTopic()+" Author: "+obj.getAuthor()+" Price: "+ obj.getPrice()+" Publisher: "+obj.getPublisher());
	}*/
	}
}
